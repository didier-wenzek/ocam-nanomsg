(jbuild_version 1)

(executables
 ((names (tests))
  (preprocess (pps (ppx_bin_prot ppx_driver.runner)))
  (libraries (oUnit bin_prot nanomsg))))

(alias
 ((name    runtest)
  (deps    (tests.exe))
  (action  (run ${<}))))
